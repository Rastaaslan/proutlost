package fr.proutlost.worldprep.pipeline;

/** Stable identifiers used on disk and across engine versions. Never persist enum ordinals. */
public enum PassId {
    TERRAIN, BIOMES, GEOLOGY, ORES, SOILS, FLORA, TREES, AQUATIC, HABITATS, VALIDATION
}
