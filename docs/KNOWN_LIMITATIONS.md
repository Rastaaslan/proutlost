# Known limitations

The current checkpoint is a domain foundation, not a production-ready environmental compiler. Minecraft terrain sampling, biome palette writes, block apply passes, SavedData persistence, incremental server scheduling, PNG output, full commands, and GameTests are not yet implemented. Do not use this build on River's map and do not interpret passing unit tests as River acceptance.
